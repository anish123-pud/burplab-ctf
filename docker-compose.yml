services:
  burplab:
    build:
      context: .
    environment:
      DEBUG: "0"
      LAB_ALLOW_LAN: "${LAB_ALLOW_LAN:-0}"
    ports:
      - "${LAB_BIND_ADDRESS:-127.0.0.1}:5000:5000"
    volumes:
      - burplab_database:/app/database
    restart: unless-stopped

volumes:
  burplab_database:
